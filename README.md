# 🎯 Трекер привычек

Полнофункциональная система управления задачами и привычками с real-time обновлениями, мотивационными элементами и продвинутой аналитикой.

## 📋 Описание

Расширенный аналог Trello или Notion, но с фокусом на построение привычек. Включает канбан-доски, календарь, напоминания, графики прогресса и мотивационные элементы (стрики, награды).

## 🚀 Технологический стек

### Backend
- **Node.js 20** - среда выполнения
- **Express** - веб-фреймворк
- **PostgreSQL** - база данных
- **Socket.io** - WebSocket для real-time обновлений
- **JWT** - аутентификация
- **bcrypt** - хеширование паролей

### Frontend
- **React 18** - UI фреймворк
- **Socket.io Client** - real-time обновления
- **Recharts** - графики и визуализация
- **React DnD** - drag-and-drop

## 📁 Структура проекта

```
habit-tracker-basnikov/
├── backend/                 # Backend сервер
│   ├── config/             # Конфигурация
│   │   ├── config.js       # Основная конфигурация
│   │   └── database.js     # Подключение к БД
│   ├── middleware/         # Middleware
│   │   └── auth.js         # JWT аутентификация
│   ├── routes/             # API маршруты
│   │   ├── auth.js         # Аутентификация
│   │   ├── boards.js       # Канбан-доски
│   │   ├── columns.js      # Колонки досок
│   │   ├── tasks.js        # Задачи
│   │   ├── habits.js       # Привычки
│   │   ├── reminders.js    # Напоминания
│   │   ├── achievements.js # Достижения
│   │   └── user.js         # Пользователь и статистика
│   ├── socket/             # WebSocket
│   │   └── socketHandler.js
│   ├── scripts/            # Утилиты
│   │   └── initDatabase.js # Инициализация БД
│   ├── utils/              # Вспомогательные функции
│   │   └── errors.js       # Обработка ошибок
│   ├── package.json
│   ├──.env.example
│   └── server.js           # Главный файл
├── database/               # SQL схемы
│   └── schema.sql
├── frontend/               # React приложение
│   └── (будет создано)
├── ARCHITECTURE.md         # Архитектура системы
├── API_DOCUMENTATION.md    # API документация
└── README.md              # Этот файл
```

## 🛠️ Установка и запуск

### Требования
- Node.js 20+ 
- PostgreSQL 14+
- npm или yarn

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd habit-tracker-basnikov
```

### 2. Настройка Backend

#### Установка зависимостей
```bash
cd backend
npm install
```

#### Настройка переменных окружения
```bash
cp.env.example.env
```

Отредактируйте `.env` файл:
```env
# Сервер
PORT=5000
NODE_ENV=development

# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=habit_tracker
DB_USER=postgres
DB_PASSWORD=your_password

# JWT
JWT_SECRET=your_super_secret_key_change_this
JWT_EXPIRE=7d

# CORS
CORS_ORIGIN=http://localhost:3000
```

#### Инициализация базы данных
```bash
# Создает базу данных, таблицы и тестовые данные
npm run init-db
```

#### Запуск backend сервера
```bash
# Режим разработки (с автоперезапуском)
npm run dev

# Продакшн режим
npm start
```

Backend сервер запустится на `http://localhost:5000`

### 3. Настройка Frontend

```bash
cd frontend
npm install
npm start
```

Frontend запустится на `http://localhost:3000`

## 📖 API Endpoints

### Аутентификация
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `GET /api/auth/me` - Текущий пользователь

### Доски и задачи
- `GET /api/boards` - Получить все доски
- `POST /api/boards` - Создать доску
- `PUT /api/boards/:id` - Обновить доску
- `DELETE /api/boards/:id` - Удалить доску
- `GET /api/boards/:id/columns` - Получить колонки доски

### Задачи
- `POST /api/tasks` - Создать задачу
- `PUT /api/tasks/:id` - Обновить задачу
- `DELETE /api/tasks/:id` - Удалить задачу
- `PUT /api/tasks/:id/move` - Переместить задачу

### Привычки
- `GET /api/habits` - Получить все привычки
- `POST /api/habits` - Создать привычку
- `PUT /api/habits/:id` - Обновить привычку
- `DELETE /api/habits/:id` - Удалить привычку
- `POST /api/habits/:id/log` - Отметить выполнение
- `GET /api/habits/:id/stats` - Статистика привычки
- `GET /api/habits/:id/streak` - Информация о стрике

### Достижения
- `GET /api/achievements` - Получить достижения
- `GET /api/achievements/available` - Доступные достижения
- `GET /api/achievements/stats` - Статистика достижений

### Пользователь
- `GET /api/user/stats` - Общая статистика
- `GET /api/user/profile` - Профиль пользователя
- `PUT /api/user/profile` - Обновить профиль
- `GET /api/user/leaderboard` - Таблица лидеров

Полная документация API: [API_DOCUMENTATION.md](./API_DOCUMENTATION.md)

## 🔌 WebSocket События

### Подключение
```javascript
import io from 'socket.io-client';

const socket = io('http://localhost:5000', {
  auth: { token: 'JWT_TOKEN' }
});
```

### События
- `task_created` - Задача создана
- `task_updated` - Задача обновлена
- `task_deleted` - Задача удалена
- `task_moved` - Задача перемещена
- `habit_completed` - Привычка выполнена
- `streak_updated` - Стрик обновлен
- `achievement_unlocked` - Достижение получено
- `level_up` - Повышение уровня

## 🎮 Основные функции

### 1. Канбан-доски
- Создание множества досок
- Drag & Drop задач между колонками
- Приоритеты и дедлайны
- Real-time синхронизация

### 2. Трекер привычек
- Ежедневные/еженедельные привычки
- Календарь выполнения
- Стрики и рекорды
- Гибкая настройка частоты

### 3. Мотивационная система
- Система очков и уровней
- Достижения за различные активности
- Стрики (серии выполнений)
- Награды и бейджи

### 4. Аналитика
- Графики прогресса по привычкам
- Статистика выполнения задач
- Тепловые карты активности
- Сравнение периодов

### 5. Напоминания
- Персональные напоминания для привычек
- Выбор времени и дней недели
- Push-уведомления в браузере

## 🏆 Система достижений

### Типы достижений
1. **Стрики**: 7, 30, 100, 365 дней подряд
2. **Количество**: 10, 50, 100 выполнений привычек
3. **Задачи**: 10, 50, 100 завершенных задач
4. **Первые шаги**: первая привычка, первая доска

### Награды
- 🥉 Бронзовые (10 очков)
- 🥈 Серебряные (25 очков)
- 🥇 Золотые (50 очков)
- 💎 Платиновые (100 очков)

## 🧪 Тестирование

```bash
# Backend тесты
cd backend
npm test

# Frontend тесты
cd frontend
npm test
```

## 🚀 Деплой

### Backend
1. Настройте PostgreSQL на production сервере
2. Установите зависимости: `npm install`
3. Инициализируйте БД: `npm run init-db`
4. Запустите сервер: `NODE_ENV=production npm start`

### Frontend
```bash
npm run build
# Деплой содержимого build/ на хостинг
```

## 📝 Тестовые данные

При инициализации базы данных создается тестовый пользователь:
- **Email**: basnikov@example.com
- **Password**: (установите при регистрации)

## 🤝 Участие в разработке

1. Fork проекта
2. Создайте feature branch (`git checkout -b feature/AmazingFeature`)
3. Commit изменения (`git commit -m 'Add some AmazingFeature'`)
4. Push в branch (`git push origin feature/AmazingFeature`)
5. Создайте Pull Request

## 📄 Лицензия

MIT License - см. файл [LICENSE](LICENSE)

## 👤 Автор

**Daniil Basnikov**
- Email: daniapuk1234@gmail.com

## 🙏 Благодарности

- Trello за вдохновение для канбан-досок
- Notion за идеи по организации данных
- Habitica за мотивационные механики

---

**Сделано с ❤️ в 2024 году**